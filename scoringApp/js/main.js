'use strict';

var iq4scoring = angular.module('iq4scoring', [
  'ui.router',
  'ngSanitize'
]);

iq4scoring.config([
  '$httpProvider',
  '$provide',
  '$sceProvider',

  function($httpProvider, $provide, $sceProvider) {
  }
    
]);
